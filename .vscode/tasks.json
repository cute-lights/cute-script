{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "meson install",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "args": [],
            "problemMatcher": ["$tsc"],

            "presentation": {
                "reveal": "always"
            },

            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}
